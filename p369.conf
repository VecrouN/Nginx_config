server {
    listen       80;
    server_name  QuartusGeneratorDev.auditory;

    #access_log  /var/log/nginx/host.access.log  main;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
    }

    location /Quartus_Generator/api/v1/QuartusProjectFiles/Pregenerated/Basic {
        root /home/p346;

        location ~ \Terasic(.+\.rar)$ {
            add_header Content-disposition "attachment; filename=$1";
        # if ($request_method = GET) {
        #     add_header 'Access-Control-Allow-Origin' "$http_origin" always;
        #     add_header 'Access-Control-Allow-Headers' "$http_access_control_request_headers" always;
        #     add_header 'Access-Control-Request-Method' "$http_access_control_request_method" always;
        #     add_header "Access-Control-Allow-Origin"  "http://localhost:8080" always;
        #     add_header "Access-Control-Allow-Origin"  "*";
        #     return 200;
        # }

            # Preflighted requests
        # if ($request_method = OPTIONS ) {
        #     add_header "Access-Control-Allow-Origin"  "http://localhost:8080";
        #     add_header "Access-Control-Allow-Credentials"  "true";
        #     add_header "Access-Control-Allow-Methods" "GET, POST, OPTIONS, HEAD";
        #     add_header "Access-Control-Allow-Headers" "withCredentials, Origin, Authorization, X-Requested-With, Content-Type, Accept";
        #     return 404;
        # }

        }

        location ~Marsohod/(.+\.rar)$ {
            add_header Content-disposition "attachment; filename=$1";
        } 
    }
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}
